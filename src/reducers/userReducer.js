// export default (state = { Users: [{first_name: 'John', last_name: 'Smith'}]}, action) => {
export default (state = [], action) => {
    
    switch (action.type) {
        
        default:
            return state
    }
  }